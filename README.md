# AutoCursorClipper

Automatically clip you cursor the specific window.

# How to use

Create shortcut to program executable and add title of the window you want to
clip as first argument. Start the program. Now when that window is bring to
foreground, cursor will be automatically clipped to the window rectangle.
To release the clip, press Alt-Tab, Windows key or minimize the window.
